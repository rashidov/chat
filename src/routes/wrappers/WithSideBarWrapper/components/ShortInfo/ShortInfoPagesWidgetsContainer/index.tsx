import React, { FC } from 'react'
import { MessengerWidget } from './widgets/Messenger'

export const ShortInfoPagesWidgetsContainer: FC = () => {
  const asd = 'asd'

  return (
    <div className="withSideBarWrapper-shortInfo-pagesWidget">
      <MessengerWidget />
    </div>
  )
}
